# Инструкция по загрузке проекта на GitHub

## Шаг 1: Распакуйте архив

Распакуйте архив `pointnet2-segmentation.zip` в любую папку.

## Шаг 2: Создайте репозиторий на GitHub

1. Зайдите на [GitHub.com](https://github.com)
2. Нажмите кнопку **"New"** (или **"+"** → **"New repository"**)
3. Заполните:
   - **Repository name**: `pointnet2-segmentation` (или любое другое имя)
   - **Description**: `PointNet++ для семантической сегментации облака точек`
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license"
4. Нажмите **"Create repository"**

## Шаг 3: Инициализируйте Git в папке проекта

Откройте терминал в папке с распакованным проектом и выполните:

```bash
# Инициализация Git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: PointNet++ segmentation project"
```

## Шаг 4: Подключите удаленный репозиторий

Скопируйте URL вашего репозитория с GitHub (кнопка "Code" → HTTPS) и выполните:

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/pointnet2-segmentation.git

# Отправка на GitHub
git branch -M main
git push -u origin main
```

**Если потребуется авторизация:**
- Используйте **Personal Access Token** (не пароль)
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
- Права: `repo` (полный доступ к репозиториям)

## Шаг 5: Проверка

Зайдите на GitHub и убедитесь, что все файлы загружены.

## Что включено в архив:

✅ Все исходные файлы проекта  
✅ `data_for_colab.zip` - данные для Colab (38MB)  
✅ `.gitignore` - исключает ненужные файлы  
✅ `README.md` - документация  
✅ `requirements.txt` - зависимости  

## Что НЕ включено (и правильно):

❌ `venv/` - виртуальное окружение (создается заново)  
❌ `__pycache__/` - кэш Python  
❌ `checkpoints/` - обученные модели  
❌ `logs/` - логи TensorBoard  
❌ Исходные PLY файлы (только архив)  

## Быстрый старт после загрузки:

```bash
# 1. Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Распаковать данные (если нужно)
unzip data_for_colab.zip

# 4. Запустить обучение
python train.py --data_dir 3011-20251217T195928Z-1-001 --area 3011 --num_classes 13
```

## Запуск в Google Colab:

1. Откройте `colab_setup.ipynb` в Google Colab
2. Выполните ячейки - все настроено автоматически
3. Данные автоматически распакуются из `data_for_colab.zip`

---

**Готово!** Проект готов к использованию. 🚀

